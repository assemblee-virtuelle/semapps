import { createMuiTheme } from '@material-ui/core/styles';

const colibrisTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#6b7e9b'
    },
    secondary: {
      main: '#bdc900'
    }
  }
});

export default colibrisTheme;
